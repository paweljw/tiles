{
  "name": "tiles",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Paweł J. Wal",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "parcel-bundler": "^1.12.3"
  },
  "scripts": {
    "start": "npm-run-all prepare server",
    "build": "npm-run-all prepare parcel-build",
    "prebuild:dir": "mkdir -p dist",
    "prebuild:atlas": "ruby build/tiles.rb > dist/tileset.json",
    "prebuild:copy": "cp src/assets/tileset.png dist/tileset.png",
    "prepare": "npm-run-all prebuild:*",
    "server": "parcel index.html",
    "parcel-build": "parcel build index.html",
    "clean": "rm -f dist/*.*"
  },
  "dependencies": {
    "pixi.js": "4.8.7"
  }
}
